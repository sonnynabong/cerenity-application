CONVEX_DEPLOYMENT=blissful-sheep-547
NEXT_PUBLIC_CONVEX_URL=https://blissful-sheep-547.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://blissful-sheep-547.convex.site
# Chat + embeddings: set one or both on the Convex deployment (not Next.js):
#   npx convex env set OPENAI_API_KEY --env-file .env.example
#   npx convex env set OPENROUTER_API_KEY --env-file .env.example
# CONVEX_DEPLOY_KEY belongs only in gitignored .env.local
# Local `npx convex dev` uses the logged-in CLI. A preview: deploy key cannot run `convex dev`.
